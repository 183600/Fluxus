// Generated by HyperStatic/CXX Compiler - DEBUG VERSION
#include <iomanip>
#include <array>
#include <tuple>
#include <chrono>
#include <atomic>
#include <functional>
#include <vector>
#include <queue>
#include <condition_variable>
#include <mutex>
#include <thread>
#include <string>
#include <iostream>

void print_vector_int(std::vector<int>& vec) {
    std::cout << "[";
    bool first = true;
    size_t i = 0;
    for (; i < vec.size(); ++i) {
    if (!first) {
    std::cout << ", ";
}
    std::cout << vec[i];
    first = false;
}
    std::cout << "]";
}

void print_array_int(std::initializer_list<int>& arr) {
    std::cout << "[";
    bool first = true;
    auto it = arr.begin();
    while (it != arr.end()) {
    if (!first) {
    std::cout << ", ";
}
    std::cout << *it;
    first = false;
    ++it;
}
    std::cout << "]";
}

std::ostream& operator<<(std::ostream& os, std::vector<int>& vec) {
    os << "[";
    bool first = true;
    for (auto it = vec.begin(); it != vec.end(); ++it) {
    if (!first) {
    os << ", ";
}
    os << *it;
    first = false;
}
    os << "]";
    return os;
}

std::ostream& operator<<(std::ostream& os, std::initializer_list<int>& lst) {
    os << "[";
    bool first = true;
    for (auto it = lst.begin(); it != lst.end(); ++it) {
    if (!first) {
    os << ", ";
}
    os << *it;
    first = false;
}
    os << "]";
    return os;
}

// Generating C++ for Go package: main
template<typename T>
struct Channel {
    std::queue<T> data_queue;
    std::mutex mutex;
    std::condition_variable cv;
    Channel() {
        // Initialize empty channel
    }
    void send(T value) {
        std::lock_guard<std::mutex> lock = std::lock_guard(this->mutex);
        this->data_queue.push(value);
        this->cv.notify_one();
    }
    T receive() {
        std::unique_lock<std::mutex> lock = std::unique_lock(this->mutex);
        this->cv.wait(lock);
        T result = this->data_queue.front();
        this->data_queue.pop();
        return result;
    }
};

// Found 1 files in package
// Processing Go file with 1 declarations
// Generating function: main
// TODO: Implement Go expression: GoIndex (Located {locSpan = SourceSpan {spanFilename = "<input>", spanStart = SourcePos {posLine = 0, posColumn = 0}, spanEnd = SourcePos {posLine = 0, posColumn = 0}}, locValue = GoIdent (Identifier "arr")}) (Located {locSpan = SourceSpan {spanFilename = "<input>", spanStart = SourcePos {posLine = 0, posColumn = 0}, spanEnd = SourcePos {posLine = 0, posColumn = 0}}, locValue = GoLiteral (GoInt 0)})
// TODO: Implement Go expression: GoIndex (Located {locSpan = SourceSpan {spanFilename = "<input>", spanStart = SourcePos {posLine = 0, posColumn = 0}, spanEnd = SourcePos {posLine = 0, posColumn = 0}}, locValue = GoIdent (Identifier "arr")}) (Located {locSpan = SourceSpan {spanFilename = "<input>", spanStart = SourcePos {posLine = 0, posColumn = 0}, spanEnd = SourcePos {posLine = 0, posColumn = 0}}, locValue = GoLiteral (GoInt 1)})
// TODO: Implement Go expression: GoIndex (Located {locSpan = SourceSpan {spanFilename = "<input>", spanStart = SourcePos {posLine = 0, posColumn = 0}, spanEnd = SourcePos {posLine = 0, posColumn = 0}}, locValue = GoIdent (Identifier "arr")}) (Located {locSpan = SourceSpan {spanFilename = "<input>", spanStart = SourcePos {posLine = 0, posColumn = 0}, spanEnd = SourcePos {posLine = 0, posColumn = 0}}, locValue = GoLiteral (GoInt 2)})
int main() {
    int a = 1;
    int b = 2;
    int c = 3;
    std::cout << "Variables:" << std::endl;
    std::cout << a << std::endl;
    std::cout << b << std::endl;
    std::cout << c << std::endl;
    auto arr = {1, 2, 3};
    std::cout << "Array elements:" << std::endl;
    std::cout << 0 << std::endl;
    std::cout << 0 << std::endl;
    std::cout << 0 << std::endl;
    return 0;
}

