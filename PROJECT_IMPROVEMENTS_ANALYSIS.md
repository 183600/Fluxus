# Fluxus 项目改进分析

## 摘要
- **C++ 渲染器重复实现且功能残缺**：`Fluxus.Compiler.Driver` 自行实现的 `renderCppUnit/renderCppStmt`（`src/Fluxus/Compiler/Driver.hs` 第 887-1086 行）忽略命名空间并对 `CppTry` 等语句输出 `// TODO` 占位符；一旦 Python 代码触发 `generatePythonTry`（`src/Fluxus/CodeGen/CPP/Python.hs` 第 599-623 行），生成的 C++ 就会缺失实际的 try/catch 逻辑。与此同时，`Fluxus.CodeGen.CPP.AST` 已提供完备渲染器（第 170-263 行），形成重复且前后不一致的实现。
- **单文件编译路径不会清理中间产物**：`compileFile` 完成链接后直接返回（`Driver.hs` 第 414-431 行），即便 `ccKeepIntermediates=False` 也不会移除 `.cpp/.o`，而 `compileProject` 则会清理（第 433-466 行）。这导致默认配置下执行一次单文件编译就会留下编译垃圾。
- **CLI 缺失对分析及“仅生成 C++”模式的切换**：配置结构体暴露了 `ccEnableAnalysis`、`ccStopAtCodegen`（`Driver.hs` 第 157-178 行），YAML 也能覆盖，但 `parseCommandLineArgs`（`Config.hs` 第 222-313 行）没有对应的 `--enable/disable-analysis`、`--stop-at-codegen` 开关，`printUsage`（`Main.hs` 第 157-227 行）亦未提示，减弱了 CLI 的可用性。

## 重点改进方向

### 1. 统一使用完整的 C++ 渲染器
- **观察**：`Driver.hs` 的渲染器忽略 `CppUnit` 中的命名空间，并在处理 `CppTry`、`CppBreak` 等节点时直接输出占位注释。与此同时，`Fluxus.CodeGen.CPP.AST` 中已经存在覆盖全面、带缩进的 `renderCppUnit/renderCppStmt` 实现。
- **影响**：Python 的 `try/except/finally`、`with` 等结构会在最终产物里全部丢失，导致编译器看似成功但生成的 C++ 不具备正确语义。
- **建议**：移除 `Driver.hs` 中的重复渲染代码，改为直接复用 `Fluxus.CodeGen.CPP.AST` 中的实现（或提取公共渲染模块并在两处共享），并补充覆盖 `CppTry`、`CppBreak` 等节点的回归测试。

### 2. 对齐单文件与项目模式的中间产物清理策略
- **观察**：`compileFile` 会把所有临时路径记录到 `csIntermediateFiles`，但仅 `compileProject` 会根据 `ccKeepIntermediates` 来删除这些文件。
- **影响**：默认配置下使用 CLI 编译单个脚本会在源目录遗留 `.cpp`、`.o` 文件，既污染仓库也影响脚本化调用（多次执行会触发 make-like 时间戳问题）。
- **建议**：在 `compileFile` 成功后复用与 `compileProject` 相同的清理逻辑，或者抽出公共辅助函数，根据 `ccKeepIntermediates` 决定是否删除临时文件。

### 3. 补全 CLI 与配置字段的一致性
- **观察**：虽然 `CompilerConfigOverrides` 支持 `enable_analysis`、`stop_at_codegen` 字段，环境变量也能覆盖，但命令行没有对应开关。
- **影响**：用户若想在一次性编译中关闭类型分析或只生成 C++，必须手写 YAML 或修改环境变量，CLI 体验不完整。
- **建议**：在 `parseCommandLineArgs` 中加入 `--enable-analysis/--disable-analysis` 与 `--stop-at-codegen`（可配对 `--full-pipeline` 还原默认）的解析分支，并更新 `printUsage` 与文档；顺带添加针对这些路径的配置优先级测试。

## 其他观察
- `renderCppUnit` 目前输出的头部注释仍写着 “Generated by HyperStatic/CXX Compiler”，若项目已更名为 Fluxus 可以同步更新品牌标识。
- `generatePythonExpr` 对尚未实现的表达式在严格模式下会记录 fatal error，但仍返回 `0` 作为占位值，建议在严格模式直接通过 `CppStmt` 终止生成，以免出现“编译成功却生成默认值”的错觉。
