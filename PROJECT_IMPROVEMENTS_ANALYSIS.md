# Fluxus 项目改进分析

## 摘要
- ❗ **C++ 渲染器重复且不完整**：`Fluxus.Compiler.Driver` 自行实现的 `renderCppUnit/renderCppDecl/renderCppStmt` 未覆盖 `CppTry`、`CppBreak` 等节点，并忽略 `CppUnit` 中的命名空间；与此同时 `Fluxus.CodeGen.CPP.AST` 已提供完备渲染器，导致生成产物出现语义缺失。
- ♻️ **单文件编译路径不会清理中间产物**：`compileFile` 在成功链接后直接返回，可执行文件旁会遗留 `.cpp/.o`；只有 `compileProject` 会依据 `ccKeepIntermediates` 清理，违背默认“清理”配置。
- 🧭 **CLI 缺少分析/代码生成阶段开关**：配置结构体与 YAML 支持 `ccEnableAnalysis`、`ccStopAtCodegen`，但 `parseCommandLineArgs` 与 `printUsage` 未暴露对应选项，用户无法通过命令行切换静态分析或“仅生成 C++”模式。
- 🔄 **生成代码仍带 HyperStatic 品牌**：`codeGenStage` 固定 `cgcNamespace = "hyperstatic"`，`Driver` 的渲染器也输出 `// Generated by HyperStatic/CXX Compiler`，与 README 中的 Fluxus 品牌不符，易引起混淆。

## 重点改进方向

### 1. 统一使用完整的 C++ 渲染器
#### 现状
- 代码位置：`src/Fluxus/Compiler/Driver.hs` (约 880-1140 行)。
- 驱动层自定义的渲染函数只处理少量 `CppDecl`/`CppStmt` 变体，对 `CppTry`、`CppCatch`、`CppBreak`、`CppContinue` 等节点直接输出 `// TODO` 注释或空字符串；同时忽略 `CppUnit` 的命名空间列表。
- `Fluxus.CodeGen.CPP.AST` 中已有完整且经过测试的渲染实现，支持所有 AST 节点并负责缩进/命名空间包装。

#### 影响
- 一旦 Python 代码包含 `try/except/finally`、`with` 等结构，`generatePythonTry` 会正确构造 `CppTry`，但驱动层渲染出的 C++ 会将整个 `try` 块替换成注释，导致编译器“成功”却生成缺失语义的源码。
- 命名空间固定缺失，使得 `cgcNamespace` 设为 `hyperstatic` 也无法体现在最终输出，破坏名称隔离策略。

#### 改进建议
- 删除或下沉驱动层的手写渲染函数，直接复用 `Fluxus.CodeGen.CPP.AST.renderCppUnit` 等入口；如需插入额外注释，可在调用前后处理。
- 增加覆盖 `CppTry`、`CppCatch` 的端到端回归测试（例如编译包含 `try`/`except` 的 Python 脚本，并断言生成的 C++ 含有 `try` 语句）。

### 2. 对齐单文件与项目模式的中间产物清理策略
#### 现状
- 代码位置：`compileFile` 与 `compileProject`（`Driver.hs` 第 414-466 行）。
- 两条路径均调用 `compileFileArtifacts` 收集中间文件，并通过 `csIntermediateFiles` 缓存；但只有 `compileProject` 会在链接后根据 `ccKeepIntermediates` 删除文件。

#### 影响
- 默认配置 (`ccKeepIntermediates = False`) 下执行 `fluxus foo.py` 会在源目录生成 `foo.cpp` 与 `foo.o`，多次运行会反复覆盖，污染仓库或脚本工作目录。
- 与 `compileProject` 的行为不一致，难以推断 CLI 的默认语义。

#### 改进建议
- 在 `compileFile` 成功返回前复用与项目模式相同的清理逻辑，可提取为 `cleanupIntermediates :: CompilerM ()` 并在两处调用。
- 补充回归测试，模拟默认配置下编译单文件并断言中间文件被移除；同时验证 `ccKeepIntermediates = True` 时仍保留产物。

### 3. 补全 CLI 与配置字段的一致性
#### 现状
- 代码位置：`src/Fluxus/Compiler/Config.hs` 中的 `CompilerConfigOverrides` 与 `parseCommandLineArgs`。
- `CompilerConfigOverrides` 支持 `enable_analysis`、`stop_at_codegen`，环境变量同样可覆盖；但命令行无 `--enable-analysis/--disable-analysis`、`--stop-at-codegen` 或等效选项，`printUsage` 亦未提示。

#### 影响
- 想要临时关闭静态分析或仅生成 C++ 的用户必须编辑 YAML 或环境变量，CLI 体验割裂。
- 难以脚本化驱动“生成 C++ 后交给外部构建系统”的场景。

#### 改进建议
- 为 `parseCommandLineArgs` 增加对应的布尔开关，例如：
  - `--enable-analysis` / `--disable-analysis`
  - `--stop-at-codegen` / `--full-pipeline`
- 更新 `printUsage`，并在 `configToArgs`/测试用例中覆盖新选项，确保配置优先级正确。

### 4. 品牌命名与生成代码标识需同步
#### 现状
- `codeGenStage` 固定 `cgcNamespace = "hyperstatic"`、`cgcHeaderGuard = "HYPERSTATIC_GENERATED"`。
- 驱动层渲染的文件头注释仍写着 “Generated by HyperStatic/CXX Compiler”。

#### 影响
- README 与文档已统一使用 Fluxus 品牌，但编译结果与日志仍出现旧名字，易让使用者误以为是不同项目或旧版本。
- 命名空间固定为 `hyperstatic` 与项目重命名冲突，若未来引入 Fluxus 命名空间策略需要额外迁移。

#### 改进建议
- 将默认命名空间、头卫士、注释同步更改为 Fluxus 相关命名（如 `fluxus`、`FLUXUS_GENERATED`）。
- 在配置中保留扩展点，允许用户自定命名空间或头卫士，以便未来按模块划分。

## 其他观察
- `renderCppUnit` 当前仍在文件头输出 HyperStatic 字样（见 `Driver.hs`），改用共享渲染器后可顺便更新品牌。
- 代码库中测试描述依旧引用 “HyperStatic Compiler Test Suite”（`test/Spec.hs`），后续可统一品牌以避免混淆。
