// Generated by HyperStatic/CXX Compiler - DEBUG VERSION
#include <iomanip>
#include <array>
#include <tuple>
#include <chrono>
#include <atomic>
#include <functional>
#include <vector>
#include <queue>
#include <condition_variable>
#include <mutex>
#include <thread>
#include <string>
#include <iostream>

void print_vector_int(std::vector<int>& vec) {
    std::cout << "[";
    bool first = true;
    size_t i = 0;
    for (; i < vec.size(); ++i) {
    if (!first) {
    std::cout << ", ";
}
    std::cout << vec[i];
    first = false;
}
    std::cout << "]";
}

void print_array_int(std::initializer_list<int>& arr) {
    std::cout << "[";
    bool first = true;
    auto it = arr.begin();
    while (it != arr.end()) {
    if (!first) {
    std::cout << ", ";
}
    std::cout << *it;
    first = false;
    ++it;
}
    std::cout << "]";
}

// Generating C++ for Go package: main
template<typename T>
struct Channel {
    std::queue<T> data_queue;
    std::mutex mutex;
    std::condition_variable cv;
    Channel() {
        // Initialize empty channel
    }
    void send(T value) {
        std::lock_guard<std::mutex> lock = std::lock_guard(this->mutex);
        this->data_queue.push(value);
        this->cv.notify_one();
    }
    T receive() {
        std::unique_lock<std::mutex> lock = std::unique_lock(this->mutex);
        this->cv.wait(lock);
        T result = this->data_queue.front();
        this->data_queue.pop();
        return result;
    }
};

// Found 1 files in package
// Processing Go file with 2 declarations
// Generating function: testSimpleMain
void testSimpleMain() {
    int a = 10;
    int b = 20;
    auto sum = a + b;
    std::cout << "Sum: " << sum << std::endl;
}

// Generating function: main
int main() {
    testSimpleMain();
    return 0;
}

